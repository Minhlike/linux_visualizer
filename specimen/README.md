# Linux specimen

P6 will pin Buildroot, Linux, BusyBox, and QEMU-compatible configuration here. Track defconfigs, root filesystem overlays, collection agents, source hashes, and license material. Do not commit generated kernels, root filesystems, VM images, or Buildroot `output/`.

The current machine does not have QEMU available in `PATH`; this does not block P0–P2 mock replay work.
